[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "diffusionfwi"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["final_src"]
include = ["diffusionfwi*"] # This ensures it picks up the package and submodules
